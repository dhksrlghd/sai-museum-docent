# 환경 변수 (절대 커밋 금지)
.env
.env.local
.env.*.local

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Node / Vite
node_modules/
frontend/dist/
frontend/.vite/

# 디버그/로그 파일
data/processed/_*.log
data/raw/_debug_*.html

# Chroma 인덱스는 git에 올리면 22MB sqlite 때문에 LFS 필요해서 제외.
# Dockerfile이 이미지 빌드 시 build_index.py로 재생성한다.
data/chroma/
data/processed/chunks.jsonl

# data/raw/ 의 작품 JSON 321개(약 4MB)는 git에 포함 (RAG 소스 자체).

# IDE / OS / 도구
.vscode/
.idea/
.claude/
.DS_Store
Thumbs.db

# AI 어시스턴트 internal handoff 문서 (CLAUDE.md)
# — 일반 독자에겐 노이즈. 정식 인수인계 정보는 docs/07-development-journey.md에 정리됨.
CLAUDE.md
src/CLAUDE.md
